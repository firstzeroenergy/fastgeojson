% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastgeojson.R
\name{fastgeojson_threads}
\alias{fastgeojson_threads}
\title{Get or set the number of worker threads}
\usage{
fastgeojson_threads(n = NULL)
}
\arguments{
\item{n}{Integer. The number of threads to use. \code{n = 1} disables parallelism
entirely, which is useful for reproducible benchmarking. \code{n <= 0} restores
the automatic behaviour described above. \code{n = NULL} (default) changes
nothing and reports the current value.}
}
\value{
An integer scalar, the number of worker threads in use after the
call. Returned invisibly when \code{n} is supplied; visibly when it is not.
}
\description{
Controls how many threads the Rust backend uses for parallel serialization.

Normal use gets the whole machine. The count is resolved in this order: an
explicit value set here; then \code{FASTGEOJSON_NUM_THREADS},
\code{RAYON_NUM_THREADS}, \code{OMP_NUM_THREADS} or \code{OMP_THREAD_LIMIT}; then two if
\verb{R CMD check} is detected (CRAN's policy caps \emph{checking} at two cores, since
the check farm is shared -- it places no limit on ordinary use); then the
number of available cores.
}
\examples{
fastgeojson_threads()
old <- fastgeojson_threads(2)
fastgeojson_threads(0)

}
